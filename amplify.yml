version: 0.1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    preBuild:
       commands:
          - cd plza-fe
          - npm install
    build:
      commands: npm run build 
  artifacts:
    baseDirectory: /plza-fe/public
    files:
      - '**/*'
  cache:
    paths: []